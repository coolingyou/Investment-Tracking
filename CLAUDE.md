# 前端开发规范
- 所有样式必须写在 `style.css` 中，严禁在 HTML 标签内写行内样式 (inline style)。
- 所有的按钮点击事件，必须加上防抖 (Debounce) 处理，防止用户重复点击提交。

# 项目核心编码规范 (Project Rules)

## 1. 技术栈与命令约束
- 包管理工具统一使用: `npm` (不使用 yarn 或 pnpm)
- 每当修改了 TypeScript 或后端代码后，必须自动运行 `npm run typecheck` 验证。
- 绝对不要在未获授权的情况下修改数据库迁移文件（如 `src/migrations/`）。

## 2. 核心代码风格 (Style Guide)
- 命名：类名使用大驼峰 (PascalCase)，方法和变量使用小驼峰 (camelCase)。
- 异常处理：严禁返回 null，必须使用明确的 Exception 捕获机制，并提供清晰的 Context。
- 注释：代码尽可能自解释。若写注释，一律使用**清晰的中文**说明逻辑背景，拒绝废话。

## 3. Claude Code 执行工作流 (Workflow)
- 修复 Bug 的标准流程：
  1. 运行测试/日志重现错误。
  2. 优先检查 `src/auth/` 等相关上下文。
  3. 编写/修改测试用例以复现错误。
  4. 实施修复，再次运行测试，验证通过后向用户报告。
- 严禁在大范围调查时直接盲目修改多处代码，必须先提出修改 Plan 供用户确认。