{
  "framework": null,
  "installCommand": "npm install",
  "buildCommand": "npm run build && cp dist/TT-logo-1.png .",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
